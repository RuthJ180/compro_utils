#!/usr/bin/env bash

set -o errexit
set -o pipefail
set -o nounset
# set -o xtrace

COREVERSION=$(drush status | grep -oP 'Drupal version.*?\:  ([\d])\.' | rev | cut -c2 || echo '0')
echo "${COREVERSION}"